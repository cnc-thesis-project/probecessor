import modules.http
import modules.ssh
import modules.rdns
import modules.unknown
import modules.tls

modules = {
    "http": modules.http,
    "ssh": modules.ssh,
    "rdns": modules.rdns,
    "unknown": modules.unknown,
    "tls": modules.tls,
}
